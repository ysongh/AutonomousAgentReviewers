# Demo Judge spike — copy to .env and fill in. .env is gitignored.

# OpenAI key for Whisper audio transcription (Stage B).
OPENAI_API_KEY=sk-...

# Anthropic key for the multimodal verdict call (Stage C).
# Same key the AAR judges use; can be copied from the repo-root .env.
ANTHROPIC_API_KEY=sk-ant-...
