{
  "name": "bootstrap-demojudge",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 1 spike for the AAR Demo Judge: mp4 -> keyframes + transcript -> one Claude multimodal call -> DemoVerdict JSON. Self-contained, NOT wired into the AAR pipeline.",
  "type": "module",
  "scripts": {
    "review": "node review-demo.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.102.0",
    "dotenv": "^17.4.2",
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "openai": "^6.42.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "ffmpeg-static"
    ]
  }
}
